{
  "name": "godicom-site",
  "private": true,
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "check": "vitepress build docs && node scripts/check-i18n.cjs",
    "serve": "vitepress serve docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
